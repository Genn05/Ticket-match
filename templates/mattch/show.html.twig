{% extends 'base.html.twig' %}

{% block title %}Mattch{% endblock %}

{% block body %}
    <h1>Mattch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mattch.id }}</td>
            </tr>
            <tr>
                <th>EquipeA</th>
                <td>{{ mattch.equipeA }}</td>
            </tr>
            <tr>
                <th>EquipeB</th>
                <td>{{ mattch.equipeB }}</td>
            </tr>
            <tr>
                <th>DateMatch</th>
                <td>{{ mattch.dateMatch ? mattch.dateMatch|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mattch_index') }}">back to list</a>

    <a href="{{ path('app_mattch_edit', {'id': mattch.id}) }}">edit</a>

    {{ include('mattch/_delete_form.html.twig') }}
{% endblock %}
