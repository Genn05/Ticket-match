{% extends 'base.html.twig' %}

{% block title %}Nouveau Paiement{% endblock %}

{% block stylesheets %}
    <style>
        .page-title {
            text-align: center;
            margin: 2rem 0;
            font-size: 2rem;
            font-weight: bold;
            color: #28a745;
        }

        .card {
            border-radius: 0.5rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
        }

        .card-header {
            background-color: #28a745;
            color: white;
            padding: 1rem;
            font-size: 1.2rem;
            font-weight: bold;
        }

        .card-body {
            padding: 1.5rem;
        }

        .btn-return {
            margin-top: 1rem;
        }
    </style>
{% endblock %}

{% block body %}
<div class="container">
    <h1 class="page-title">💳 Ajouter un Nouveau Paiement</h1>

    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card">
                <div class="card-header">
                    Formulaire de Paiement
                </div>
                <div class="card-body">
                    {{ include('paiement/_form.html.twig', {'button_label': 'Valider'}) }}

                    <a href="{{ path('app_paiement_index') }}" class="btn btn-outline-secondary btn-return">
                        <i class="fas fa-arrow-left me-1"></i> Retour à la liste
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
