{% extends 'base.html.twig' %}

{% block title %}Liste des Paiements{% endblock %}

{% block nav %}
    {% include 'partials/nav.html.twig' %}
{% endblock %}

{% block body %}
<div class="container py-4">
    <h1 class="page-title">💳 Historique des Paiements</h1>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="thead-light">
                <tr>
                    <th>Id</th>
                    <th>Date de Paiement</th>
                    <th>Montant (€)</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for paiement in paiements %}
                <tr>
                    <td>{{ paiement.id }}</td>
                    <td>{{ paiement.datePaiement ? paiement.datePaiement|date('d/m/Y H:i') : '' }}</td>
                    <td>{{ paiement.montant }}</td>
                    <td>
                        {% if paiement.status == 'Payé' %}
                            <span class="badge badge-success">Payé</span>
                        {% else %}
                            <span class="badge badge-danger">Non Payé</span>
                        {% endif %}
                    </td>
                    <td class="actions">
                        {% if paiement.reservation %}
<a href="{{ path('app_paiement_show', {'id': paiement.id}) }}" class="btn btn-sm btn-outline-primary btn-action">
                                <i class="fas fa-eye"></i> Voir
                            </a>
                        {% else %}
                            <span class="text-muted">Pas de réservation</span>
                        {% endif %}
                        <!-- Other actions -->
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="no-records">Aucun paiement trouvé.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="mt-3">
        <a href="{{ path('app_paiement_new') }}" class="btn btn-create">
            <i class="fas fa-plus-circle"></i> Nouveau Paiement
        </a>
    </div>
</div>
{% endblock %}


